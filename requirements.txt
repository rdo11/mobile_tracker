# Core (lazy imports keep the app runnable without the heavy stack)
opencv-python>=4.8
PyYAML>=6.0
numpy>=1.24
fastapi>=0.110
uvicorn[standard]>=0.29

# Detection + tracking (YOLOv8, ByteTrack via ultralytics)
ultralytics>=8.2

# Vehicle attribute classification (optional deep models)
torch>=2.1
torchvision>=0.16

# OCR engines (optional — pick one; app falls back to blur-only if absent)
easyocr>=1.7
# paddleocr>=2.7          # alternative OCR engine — uncomment to use PaddleOCR
